import React from 'react';
import ReactDOM from 'react-dom/client';
import CallAgent from './call_agent_component.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CallAgent />
  </React.StrictMode>
);